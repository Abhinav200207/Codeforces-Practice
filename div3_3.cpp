#include <bits/stdc++.h>
using namespace std;

int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        int n;
        cin >> n;
        string a,b;
        cin >> a >> b;
        if (a[0] == b[0]){
            cout << "YES\n";
            cout << a[0] << "*\n";
        }else if (a[n - 1] == b[n - 1]){
            cout << "YES\n";
            cout << "*" << a[n - 1] << "\n";
        }else{
            for (int i = 0; i < n - 1; i++){
                if (a[i] + )
            }
        }
    }
}