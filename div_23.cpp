#include <bits/stdc++.h>
using namespace std;

int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        int n;
        cin >> n;
        vector<int> v1;
        vector<int> v2;
        vector<pair<int,int>> v;
        for (int i = 0; i < n; i++){
            cin >> v1[i];
        }
        for (int i = 0; i < n; i++){
            cin >> v2[i];
        }
        for (int i = 0; i < n; i++){
            v[i] = make_pair(v1[i], v2[i]);
        }
        sort(v.begin(), v.end());
        int i = 0; i = n - 1;
        while (i < j){
            
        }
    }
}